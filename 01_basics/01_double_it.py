curr_value = int(input("Enter a number: "))

# Double the number until it reaches 100 or greater
while curr_value < 100:
    # Double the value
    curr_value = curr_value * 2
    # Print the new value
    print(curr_value, end=' ')
